{
  "name": "sophisticated-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Fewer, better tools. The essentials.",
  "scripts": {
    "start": "npx --yes serve . -l ${PORT:-3000}",
    "dev": "npx --yes serve . -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
