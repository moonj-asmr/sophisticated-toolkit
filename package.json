{
  "name": "sophisticated-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Fewer, better tools. The essentials.",
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "start": "npm run build && npx --yes serve . -l ${PORT:-3000}",
    "dev": "npx --yes serve . -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
